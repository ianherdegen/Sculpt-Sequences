
  # Yoga Sequence Builder Prototype

  This is a code bundle for Yoga Sequence Builder Prototype. The original project is available at https://www.figma.com/design/Fu4L3d49olc1ETOmWHSFMj/Yoga-Sequence-Builder-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  